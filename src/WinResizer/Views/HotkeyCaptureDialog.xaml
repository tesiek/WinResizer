<Window x:Class="WinResizer.Views.HotkeyCaptureDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Width="440"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource WindowForeground}"
        Loaded="Dialog_Loaded"
        Closing="Dialog_Closing">
  <Grid Margin="20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="ActionText"
               FontSize="16"
               FontWeight="SemiBold"
               Margin="0,0,0,8" />
    <TextBlock Grid.Row="1"
               Text="Press the desired modifier and key combination."
               Margin="0,0,0,12" />
    <Border Grid.Row="2"
            Padding="12"
            Margin="0,0,0,16"
            BorderThickness="1"
            BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
            Background="{DynamicResource ControlFillColorDefaultBrush}">
      <TextBlock x:Name="CurrentKeyText"
                 Text="Waiting for input…"
                 FontSize="18"
                 FontWeight="SemiBold"
                 HorizontalAlignment="Center"
                 TextAlignment="Center" />
    </Border>
    <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
      <ui:Button x:Name="ClearButton"
                 Content="Clear"
                 Visibility="Collapsed"
                 Style="{StaticResource ToolbarButtonLayoutStyle}"
                 Click="Clear_Click" />
      <ui:Button Content="Cancel"
                 IsCancel="True"
                 Style="{StaticResource ToolbarButtonLayoutStyle}"
                 Margin="0" />
    </StackPanel>
  </Grid>
</Window>
